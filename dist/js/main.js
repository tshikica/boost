(function($) {
    scrollCue.init();
    $(".owl-carousel").owlCarousel({

        items : 1,
        loop: true,
        autoplay:true,
        autoplaySpeed: 500,
        dots: true,
        // autoHeight: true,

    });
})(jQuery)